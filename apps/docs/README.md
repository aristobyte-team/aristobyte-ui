# @aristobyte-ui/docs

AristoByteUI documentation site built with Next.js (App Router).

## 🚀 Getting Started

From the repo root:

```bash
yarn dev:docs
```

Open http://localhost:3000 to view the docs.

## 🧰 Scripts

- `dev`: Next.js dev server (port 3000)
- `build`: production build
- `start`: production server
- `lint`: Next.js lint

## 📝 Notes

- Docs render previews from local workspace packages.
- Code blocks are loaded via `/api/code`.

## 🚀 Deploy

Docs are deployed to GitHub Pages.
