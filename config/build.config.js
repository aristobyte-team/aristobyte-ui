'use strict';

const path = require('path');

module.exports = {
  APP_DIR: path.resolve('src', 'main'),
  JS_DIR: path.join('static', 'js'),
  CSS_DIR: path.join('static', 'css'),
  MEDIA_DIR: path.join('static', 'media'),
};
