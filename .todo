- fix the deps in apps/docs so it's aligned with the global yarn, decide or ask GPT if it should be added to global package.json with -W or into @aristobyte-ui/docs using workspace add
- fix add index.d.ts file in each package to have a typescript recognision.
- fix: either change the path in dist by removing the ./src from dist and leaving the inner files or export from package.json proper files not only just 'es' and 'lib'
